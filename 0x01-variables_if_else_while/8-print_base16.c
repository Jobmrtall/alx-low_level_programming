#include <stdio.h>
/**
 * main - Entry point
 * description: 'print both upper and lowercase'
 * Return: always 0
 */
int main(void)
{
	int n;
	int k;

	for (n == 48; k <= 57; n++)
	{
		putchar(n);
	}
	for (k == 97; k <= 102; k++)
	{
		putchar(k);
	}
	putchar ('\n');
	return (0);
}
